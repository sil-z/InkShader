"""Desktop shell (pywebview native editor windows).

Only the `desktop` build mode imports anything from this package; the `web`
build serves the frontend to the system browser and carries no GUI dependency.
"""
