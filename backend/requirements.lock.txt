# InkShader 依赖锁定（直接依赖）
#
# 记录在 Windows 10 / Python 3.11.9 上实际验证通过、并被打入单文件产物的一组
# 版本。依赖范围由 backend/pyproject.toml 声明（下限），本文件给出精确值。
#
# 安装：
#   python -m pip install -r backend/requirements.lock.txt
#
# 本文件只固定直接依赖，不含传递依赖闭包。需要完整闭包时，在干净虚拟环境里：
#   python -m venv .venv
#   .venv/bin/python -m pip install -e ./backend
#   .venv/bin/python -m pip freeze > backend/requirements.lock.txt
#
# 内容与安装环境一致：更改锁定版本后需重新运行 test/probe_*.mjs 回归并重新打包。

# --- 字体管线 ---
fonttools==4.61.1
ufo2ft==3.7.0
ufoLib2==0.18.1
booleanOperations==0.9.0
brotli==1.2.0
skia-pathops==0.9.2

# --- API 服务 ---
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32

# --- 桌面壳（仅 desktop 模式；web 控制台后端不导入，打包时被排除）---
pywebview==6.2.1

# --- 打包期（仅构建可执行文件时需要）---
pyinstaller==6.22.2
Pillow==12.2.0
