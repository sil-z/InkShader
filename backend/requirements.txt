# InkShader 后端运行时依赖（见 BACKEND_RESEARCH.md）
# 安装：python -m pip install -r backend/requirements.txt
# 本机已验证版本（Python 3.11.9, Windows）：

# --- 字体管线 ---
fonttools>=4.61.1          # 核心（feaLib / ttLib / cu2qu / WOFF2）
ufo2ft>=3.7.0              # UFO -> OTF/TTF 编译
ufoLib2>=0.18.1            # UFO（含 zip）读取
booleanOperations>=0.9.0   # 源字形 remove overlap
brotli>=1.2.0              # WOFF2 压缩
skia-pathops>=0.9.2        # remove overlap 快速后端（ttLib.removeOverlaps / overlapsBackend="pathops"）

# --- API 服务 ---
fastapi>=0.136.3
uvicorn>=0.49.0
python-multipart>=0.0.32   # FastAPI UploadFile（UFO zip 上传）
