// ESM entry for svgpath.
//
// svgpath.min.js is an ES module build, so it needs no <script> tag. This entry
// exists to keep one import convention for every vendored library
// (`import svgpath from '../../vendor/svgpath.js'`).
//
// See js/vendor/README.md for the file list, versions and upgrade steps.

export { default } from './svgpath.min.js';
